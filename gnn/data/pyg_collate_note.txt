In the definition of the function `_collate` from `torch_geometric.data.collate`,
it was necessary to change

```
if increment:
    incs = get_incs(key, values, data_list, stores)
    if incs.dim() > 1 or int(incs[-1]) != 0::
        values = [
            value + inc.to(value.device)
            for value, inc in zip(values, incs)
        ]
```

to

```
if increment:
    incs = get_incs(key, values, data_list, stores)
    if incs.dim() > 1:
        values = [
            value + inc.to(value.device)
            for value, inc in zip(values, incs)
        ]
```